package ${PACKAGE}.domain.${ENTITY_LOWERCASE}

import java.util.UUID

interface ${ENTITY}WriteRepo {

    /** Сгенерировать новый id */
    suspend fun getNextId(): ${ENTITY}Id = ${ENTITY}Id(UUID.randomUUID())

    /** Создать запись */
    suspend fun create(id: ${ENTITY}Id, createModel: Create${ENTITY}Model): ${ENTITY}Model

    /** Получить запись */
    suspend fun get(id: ${ENTITY}Id): ${ENTITY}Model?
}
