package ${PACKAGE}.domain.${ENTITY_LOWERCASE}

interface ${ENTITY}WriteRepo {
    /** Сгенерировать новый id */
    suspend fun getNextId(): ${ENTITY}Id

    /** Получить запись по id */
    suspend fun get(${ENTITY_LOWERCASE}Id: ${ENTITY}Id): Result<${ENTITY}Model?>

    /** Создать запись */
    suspend fun create(${ENTITY_LOWERCASE}: Create${ENTITY}Model): Result<${ENTITY}Model>

    /** Удалить запись */
    suspend fun delete(id: ${ENTITY}Id): Result<Boolean>

    /** Очистить репозиторий */
    suspend fun cleanup(): Result<Boolean>
}
