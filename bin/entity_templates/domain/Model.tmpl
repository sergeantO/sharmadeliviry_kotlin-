package ${PACKAGE}.domain.${ENTITY_LOWERCASE}

import java.util.UUID

@JvmInline value class ${ENTITY}Id(
    val value: UUID,
)

data class ${ENTITY}Model(
    val id: ${ENTITY}Id,
    val name: String,
)

data class Create${ENTITY}Model(
    val name: String,
)
