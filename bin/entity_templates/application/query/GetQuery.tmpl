package ${PACKAGE}.application.query.${ENTITY_LOWERCASE}

import ${PACKAGE}.domain.${ENTITY_LOWERCASE}.Create${ENTITY}Model
import ${PACKAGE}.application.${ENTITY_LOWERCASE}.${ENTITY}ReadRepo
import ${PACKAGE}.domain.${ENTITY_LOWERCASE}.${ENTITY}Model

class Get${ENTITY}Command {
    suspend fun execute(${ENTITY_LOWERCASE}ReadRepo: ${ENTITY}WriteRepo, id: ${ENTITY}Id): ${ENTITY}Model? {
        val result = ${ENTITY_LOWERCASE}ReadRepo.get(id)
        return result
    }
}
